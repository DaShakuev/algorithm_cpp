# Coverage Path Planning for USV Mission Design

Алгоритм построения маршрута покрытия произвольной области для беспилотного надводного аппарата (USV).

Репозиторий содержит реализацию геометрического алгоритма генерации галсов (tacks) и формирования непрерывного обзорного маршрута внутри заданного полигона.

---

## 📌 Назначение

Алгоритм предназначен для:
- планирования обзорных миссий;
- покрытия произвольных (в том числе невыпуклых) областей;
- генерации waypoint'ов для навигационной системы аппарата;
- интеграции в GUI наземной станции управления.

---

## 🧠 Как работает алгоритм

1. На основе границ области строятся кандидатные галсы.
2. Для каждого галса вычисляется критерий покрытия.
3. Выбирается оптимальный сегмент (жадный критерий).
4. Покрытая область вычитается из исходного полигона.
5. Процесс повторяется до полного покрытия.
6. Галсы связываются в непрерывный маршрут.

Используется библиотека **Shapely** для:
- булевых операций над полигонами,
- поиска пересечений,
- вычисления расстояний.

---

## 📂 Структура

- `coverage_path_planning_algorithm.py`  
  Реализация алгоритма покрытия области и визуализации.

- `tack.py`  
  Класс `Tack` — связный список галсов и логика их последовательного соединения.

---


## Пример интеграции алгоритма в GUI для планирования обзорной миссии беспилотного надводного аппарата

<p align="center">
  <img src="gui.png" width="850"/>
</p>

---

## ⚙️ Зависимости

```
pip install numpy matplotlib shapely
```

---

## 🚀 Пример использования

```
vertices = [(4, 11), (11, 7), (16, 12), (13, 16), (8, 17)]
track_width = 1

points, ax = coverage_path_planning_algorithm(vertices, track_width)
```

`points` — последовательность waypoint'ов для передачи в навигационный модуль аппарата.

---

## 🎯 Результат

Алгоритм формирует:
- геометрически корректный маршрут покрытия;
- упорядоченный список точек движения;
- основу для интеграции с автопилотом или GUI миссионного планировщика.

---

Проект демонстрирует базовую реализацию coverage path planning для морских робототехнических систем.